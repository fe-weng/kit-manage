# 交接文档 — 2026-09-07

## 今日完成

### D1-D3 积分域安全修复（T1-T4）

| Commit | 内容 |
|---|---|
| `c17e162` | **T1 Domain 层校验加固**: PointBalance.deduct() 移除 Math.max(0,...) 截断允许负值；spendOnPet/spendOnReward 增加 amount>0 校验；Reward.create/update 增加 points>0 校验 |
| `5c7b8e3` | **T2 券退还功能**: RewardLog 新增 `returned` 状态 + `markReturned()` 方法；PointBalance 新增 `refundReward(amount)`；PointService/RewardService/useRewardStore 全链路实现 |
| `2b02693` | **T3 我的券页面退还按钮**: CouponCard 新增退还按钮（灰色描边），复用 ConfirmDialog 弹窗二次确认，显示返还积分数 |
| `f3c0abc` | **T4 取消任务负分引导弹窗**: 新建 NegativeBalanceGuide 组件；取消正分任务检测余额是否变负；有待使用券时引导先去退券；支持"仍然取消"允许负分 |

### Bug 修复

| Commit | 内容 |
|---|---|
| `3ade39b` | **B1+B2 修复**: RewardEditModal 中 `useEffect` 依赖了 `categories`，自定义分类操作会触发表单全部重置。移除 `categories` 依赖，仅弹窗打开时初始化表单 |
| `67d2377` | **备份链路适配 categories 表**: 09-04 新增 `categories` 表后，export/import/reset/validate 均未适配。全部补齐 |
| `b295aaf` | **兑换重复显示成功**: `handleRedeemConfirm` 中 `if(ok)` 判断对象 truthy，兑换失败也显示成功弹窗。改为 `if(result.success)` + toast 失败提示 + RewardCard 已有券禁用状态 |
| `3b8d1a3` | **我的券按钮文案**: "已使用"改为"点击使用"，避免歧义 |
| `1a2a840` | **自定义分类表单重置 + 保存竞态**: 表单初始化与 ESC 监听拆分为独立 useEffect（断开 onClose 引用链）；保存按钮 onMouseDown preventDefault 消除 blur/save 并发竞态；handleSave 主动提交未确认的自定义分类 |
| `a83263e` | **P1-P4 并发竞态防护**: TodayTaskList/TaskCheckin/Pet/Shop 四处快速连点竞态，统一用 useRef 同步锁 + useState UI 双重机制修复 |
| `c2752e4` | **P1 简单项修复**: Store fetch try/finally、Task NEGATIVE 规范化、Settings fetchPet、CreatePetForm try/finally、Toast timer |
| `3bbc727` | **P1 中等项修复**: EvolutionOverlay useRef 稳定化、Home loading+宠物引导、TaskLog sortBy |
| `6210800` | **P1-10 mutation 错误反馈**: TaskManage/PetNameEditor/MyCoupons 加 try/catch + toast 提示 |
| `9614c0f` | **全局命令式 Toast**: 新建 shared/toast.ts + GlobalToast 组件，迁移 5 页面到 `toast.error/success` |

### 新功能

| Commit | 内容 |
|---|---|
| `5377adf` | **首页待使用券迷你卡 CouponMiniCard**: Grill-Me Q14 决策实现。clay-morphism 风格，显示待使用券数量和最新券标题，点击跳转我的券页面。仅有 pending 券时显示 |

### Task History 打卡历史功能（T1-T9）

| Commit | 内容 |
|---|---|
| T1+T2 | **DailyTaskSnapshot 模型 + Dexie 持久化**: 领域模型(create/addTask/removeTask/toJSON)，ISnapshotRepository 接口，DexieSnapshotRepository 实现，IndexedDB v6 dailySnapshots 表 |
| `361cf60` | **T3 快照生成/更新/查询**: ensureTodaySnapshot 首次访问生成今日快照，updateTodaySnapshot 任务增删自动更新，getMonthSnapshots/getMonthLogs 月度查询 |
| `7d2effe` | **T4 MonthCalendar 月历组件**: 基于 react-day-picker v10 + date-fns，自定义 Day 组件，getCellStatus 回调返回状态字符串映射颜色圆圈，中文 locale + 周一起始 |
| `bb720bd` | **T5 TaskHistoryPage 主页面**: 路由 /tasks/history，Segment 切换(按日期/按任务)，TaskCheckin Header 新增「历史」入口 |
| `8482d25` | **T6 DateView 按日期视图**: useTaskHistoryStore 数据管理，4 色圆圈(全完成/≥50%/<50%/未完成)，点击日期展开任务完成详情 |
| `75eaed1` | **T7 TaskView 按任务视图 + 时区修复**: 任务 Chips 选择器 + 单任务统计，修复 YYYY-MM-DD 解析时区 Bug（parseDateStrToLocal/getDayRange） |
| `d9b8586` | **fix: DayPicker v10 Day 组件修复**: v10 非交互模式不渲染 DayButton，改用自定义 Day(td) 组件 |
| `f02c811` | **style: 颜色圆圈包裹日期**: 32px 圆形背景替代 6px 小圆点，状态映射 CSS class |
| `3f5e103` | **T8+T9 月份选择器 + 月统计**: MonthPickerModal 弹窗(年月快选)，MonthStats 卡片(完成率/全勤天数/总积分) |

---

## 排查记录

### 数据丢失排查
- 现象: 今日启动发现任务和宠物数据为空，商城数据存在
- IndexedDB 检查结果: tasks/taskLogs/pets/rewardLogs 均为空，rewards(8条)/categories(8条) 有数据
- 根因: 用户之前触发了设置页的「数据重置」，`resetAll()` 清空了 6 张表但遗漏了 `categories`（已在 `67d2377` 修复）
- 商城"看起来有数据": `initPresetRewards()` 在 ShopPage 加载时自动创建了 6 个预设奖励

---

## 当前已知问题（全项目审查 2026-09-07）

> 09-04 文档中的 B1、B2 已在本次修复

### P0 — 阻断级

| # | 问题 | 文件 | 状态 |
|---|------|------|------|
| D1 | ~~积分守恒被破坏~~ | `PointBalance.ts` | ✅ T1 已修复（允许负值 + 券退还机制） |
| D2 | ~~deduct 静默截断~~ | `PointBalance.ts` | ✅ T1 已修复（移除 Math.max） |
| D3 | ~~奖励积分无正数校验~~ | `Reward.ts` | ✅ T1 已修复（create/update 校验） |
| P1 | ~~任务打卡并发竞态~~ | `TodayTaskList.tsx` | ✅ `a83263e` 已修复（useRef 同步锁） |
| P2 | ~~TaskCheckin processing 闭包竞态~~ | `TaskCheckin/index.tsx` | ✅ `a83263e` 已修复 |
| P3 | ~~喂食并发竞态~~ | `Pet/index.tsx` | ✅ `a83263e` 已修复 |
| P4 | ~~兑换并发竞态~~ | `Shop/index.tsx` | ✅ `a83263e` 已修复 |

### P1 — 重要

| # | 问题 | 文件 | 状态 |
|---|------|------|------|
| 1 | ~~Store fetch 无 try/finally~~ | 所有 `use*Store.ts` | ✅ `c2752e4` |
| 2 | Dexie 升级缺 `.upgrade()` 迁移 | `DexieDatabase.ts` | ⬜ 待上线前处理 |
| 3 | ~~Task.update() NEGATIVE 符号规范化~~ | `Task.ts` | ✅ `c2752e4` |
| 4 | 备份 validate 不兼容旧版本 | `JsonBackupAdapter.ts` | ⬜ 跳过（第一版无历史数据） |
| 5 | ~~进化遮罩 onDone 引用不稳定~~ | `EvolutionOverlay.tsx` | ✅ `3bbc727` |
| 6 | ~~设置页未调用 fetchPet()~~ | `Settings/index.tsx` | ✅ `c2752e4` |
| 7 | ~~创建宠物 submitting 无 try/finally~~ | `CreatePetForm.tsx` | ✅ `c2752e4` |
| 8 | ~~首页无 loading 态、无宠物引导~~ | `Home/index.tsx` | ✅ `3bbc727` |
| 9 | ~~Toast timer 未清理~~ | 多个页面 | ✅ `9614c0f` 全局 Toast 重构 |
| 10 | ~~多处 mutation 无错误反馈~~ | TaskManage/PetNameEditor/MyCoupons | ✅ `6210800` + `9614c0f` |
| 11 | ~~TaskLog 查询无排序~~ | `DexieTaskRepository.ts` | ✅ `3bbc727` |
| 12 | ~~TS 编译错误~~ | `Shop/index.tsx` | ✅ `9ce6d73` |

### P2 — 建议（精选）

| 问题 | 文件 |
|------|------|
| 饥饿度机制未完整实现 | `Pet.ts` |
| Category 仅 interface，形态不一致 | `Category.ts` |
| 文档过期（ARCHITECTURE 写 v2/6 表） | `.cursor/context/` |
| MyCoupons 重复实现 getTodayStart | `MyCoupons/index.tsx` |
| 宠物图片映射不完整 | `PetDisplay.tsx` |
| Suspense 无 fallback | `App.tsx` |
| Modal 可访问性不完整 | 多个弹窗组件 |

---

## 待完成功能

### 中优先级
| 任务 | 说明 |
|---|---|
| 补充宠物素材 | rabbit 全阶段、chicken 4-5 阶段 |
| 样式方案统一 | inline style vs Tailwind 并存 |

### 低优先级
| 任务 | 说明 |
|---|---|
| PWA 图标替换 | 当前 SVG 占位，Prompt 见 `docs/pwa-icon-prompt.md` |
| Dead code 补清 | `review-2026-09-03.md` 中仍有 ~10 项未清理方法 |

---

## 关键文件索引（本次变更）

| 文件 | 说明 |
|---|---|
| `src/domain/models/PointBalance.ts` | T1: deduct 允许负值 + refundReward + spend 校验 |
| `src/domain/models/Reward.ts` | T1: create/update points>0 校验 |
| `src/domain/models/RewardLog.ts` | T2: 新增 returned 状态 + markReturned() |
| `src/application/services/PointService.ts` | T2: 新增 refundReward(amount) |
| `src/application/services/RewardService.ts` | T2: 新增 returnCoupon(logId) |
| `src/presentation/hooks/useRewardStore.ts` | T2: 新增 returnCoupon(logId) |
| `src/presentation/pages/MyCoupons/index.tsx` | T3: 退还按钮 + 弹窗确认 + 按钮文案调整 |
| `src/presentation/pages/TaskCheckin/index.tsx` | T4: 负分拦截逻辑 |
| `src/presentation/pages/TaskCheckin/NegativeBalanceGuide.tsx` | **新增** T4: 负分引导弹窗 |
| `src/presentation/pages/Shop/RewardEditModal.tsx` | B1+B2 修复 + useEffect 拆分 + 保存竞态修复 |
| `src/infrastructure/storage/JsonBackupAdapter.ts` | categories 加入备份链路 |
| `src/domain/repositories/IBackupAdapter.ts` | BackupData 加 categories 字段 |
| `src/presentation/pages/Home/CouponMiniCard.tsx` | **新增** 待使用券迷你卡 |
| `src/presentation/pages/Home/index.tsx` | 集成 CouponMiniCard + fetchPendingCoupons |
| `src/presentation/pages/Shop/index.tsx` | 兑换成功判断修复 + toast + pendingRewardIds |
| `src/presentation/pages/Shop/RewardCard.tsx` | hasPending prop + "已有券"状态 |
| `src/presentation/pages/Home/TodayTaskList.tsx` | P1: TaskRow useRef 锁 |
| `src/presentation/pages/Pet/index.tsx` | P3: feedingRef 双重锁 |
| `src/shared/toast.ts` | **新增** 全局命令式 Toast API |
| `src/presentation/components/GlobalToast.tsx` | **新增** 全局 Toast 渲染组件 |

---

### Task History 关键文件

| 文件 | 说明 |
|---|---|
| `src/domain/models/DailyTaskSnapshot.ts` | **新增** 每日任务快照领域模型 |
| `src/domain/repositories/ISnapshotRepository.ts` | **新增** 快照仓库接口 |
| `src/infrastructure/database/repositories/DexieSnapshotRepository.ts` | **新增** Dexie 快照仓库实现 |
| `src/infrastructure/database/DexieDatabase.ts` | v6: 新增 dailySnapshots 表 |
| `src/domain/rules/DateUtils.ts` | 新增 parseDateStrToLocal/getDayRange/getMonthRange 等 |
| `src/application/services/TaskService.ts` | 新增 ensureTodaySnapshot/updateTodaySnapshot/getMonthSnapshots/getMonthLogs |
| `src/presentation/components/MonthCalendar.tsx` | **新增** 通用月历组件（react-day-picker v10） |
| `src/presentation/hooks/useTaskHistoryStore.ts` | **新增** 历史数据 Zustand store |
| `src/presentation/pages/TaskHistory/index.tsx` | **新增** 打卡历史主页面（Segment + 日历 + 统计） |
| `src/presentation/pages/TaskHistory/DateView.tsx` | **新增** 按日期视图（任务完成详情） |
| `src/presentation/pages/TaskHistory/TaskView.tsx` | **新增** 按任务视图（Chips + 统计） |
| `src/presentation/pages/TaskHistory/MonthPickerModal.tsx` | **新增** 月份选择器弹窗 |
| `src/presentation/pages/TaskHistory/MonthStats.tsx` | **新增** 月统计摘要 |

---

## 设计文档
- 宠物系统决策: `docs/grill-me-2026-09-04-pet-system.md`
- 商城系统决策: `docs/grill-me-2026-09-04-shop-system.md`
- 打卡历史设计: `docs/grill-me-2026-09-07-task-history.md`
