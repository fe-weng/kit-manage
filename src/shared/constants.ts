/** 默认小孩 ID（单用户模式下使用） */
export const DEFAULT_CHILD_ID = 'default'

/** 奖励分类列表 */
export const REWARD_CATEGORIES = ['娱乐', '美食', '玩具', '特权', '其他'] as const

/** 路由路径 */
export const ROUTES = {
  HOME: '/home',
  TASKS: '/tasks',
  TASKS_MANAGE: '/tasks/manage',
  PET: '/pet',
  SHOP: '/shop',
  SETTINGS: '/settings',
} as const
